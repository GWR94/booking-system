# =============================================================================
# Booking System – Next.js fullstack app
# Copy to .env and fill in values. Required vars are needed for app to start.
# =============================================================================

# -----------------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------------
# For production/Neon etc. add: ?sslmode=verify-full (avoids pg SSL warning; use verify-full for strict TLS)
DATABASE_URL=postgresql://user:password@localhost:5432/booking_db
AUTH_SECRET=generate-with-openssl-rand-base64-32
STRIPE_SECRET_KEY=sk_test_...

# -----------------------------------------------------------------------------
# Stripe (webhook required for payment confirmation)
# -----------------------------------------------------------------------------
STRIPE_WEBHOOK_SECRET=whsec_...

# -----------------------------------------------------------------------------
# Public (exposed to browser; prefix with NEXT_PUBLIC_)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
NEXT_PUBLIC_FRONT_END=http://localhost:3000
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_CAPTCHA_SITE_KEY=
# Server-side reCAPTCHA verification (guest checkout); or use CAPTCHA_SECRET_KEY
RECAPTCHA_SECRET_KEY=
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=

# -----------------------------------------------------------------------------
# URLs (emails, redirects, Stripe success/cancel). One var for server and client.
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000
LOGO_URL=

# -----------------------------------------------------------------------------
# Admin / cron
# -----------------------------------------------------------------------------
ACCESS_TOKEN_SECRET=optional-for-admin-password-reset-emails
CRON_SECRET=optional-for-cron-cleanup-route

# -----------------------------------------------------------------------------
# SMTP (contact form, password reset emails)
# -----------------------------------------------------------------------------
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=

# -----------------------------------------------------------------------------
# OAuth (optional; omit to disable a provider)
# -----------------------------------------------------------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FACEBOOK_CLIENT_ID=
FACEBOOK_APP_SECRET=
TWITTER_CLIENT_ID=
TWITTER_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Stripe membership price IDs (optional; for subscription tiers)
# -----------------------------------------------------------------------------
STRIPE_PRICE_ID_PAR=
STRIPE_PRICE_ID_BIRDIE=
STRIPE_PRICE_ID_HOLEINONE=
