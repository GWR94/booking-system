refactor: migrate to full-stack Next.js app (replace Vite + Express backend)

BREAKING: Single Next.js app replaces separate Vite SPA and Express API.
All backend functionality is now in Next.js API routes and server modules.
booking-system-backend can be retired.

## Framework & build
- Replace Vite + React SPA with Next.js 16 (App Router)
- Remove React Router; use Next.js file-based routing under src/app/
- Add next.config.mjs, next-env.d.ts; retain Vitest for tests
- Update tsconfig paths for Next (e.g. @auth, @db, @modules)
- Add Prisma (schema, client under src/server/db) for DB access

## Auth
- Replace JWT/Passport with NextAuth v5 (Auth.js); session strategy JWT
- Add src/auth.ts and src/auth.config.ts; API route at /api/auth/[...nextauth]
- Add src/server/auth/auth.ts with isAdmin() and getSessionUser()
- Frontend: auth API paths updated (/verify → /api/user/me, etc.)
- OAuth (Google, Facebook) and credentials provider; optional Twitter

## API layer (backend-in-Next)
- New API routes under src/app/api/:
  - Auth: /api/auth/[...nextauth], /api/user/login, /api/user/me (GET/PATCH),
    /api/user/register, /api/user/check-email, /api/user/request-password-reset,
    /api/user/reset-password, /api/user/profile/delete,
    /api/user/social-connection/[provider] (DELETE),
    /api/user/subscription/create-session, /api/user/subscription/portal-session
  - Bookings: /api/bookings (POST), /api/bookings/[id]/cancel (POST),
    /api/bookings/payment-intent (POST), /api/bookings/payment/[paymentId] (GET)
  - Slots: /api/slots (GET)
  - Admin: /api/admin/users (GET), /api/admin/users/[id] (PUT),
    /api/admin/users/[id]/reset-password (POST),
    /api/admin/bookings (GET), /api/admin/bookings/local-book (POST),
    /api/admin/bookings/[id] (PATCH/DELETE), /api/admin/bookings/[id]/status (PATCH),
    /api/admin/bookings/[id]/extend (PATCH), /api/admin/bookings/[id]/extend-availability (GET),
    /api/admin/slots (GET/POST), /api/admin/slots/[id] (PUT/DELETE),
    /api/admin/slots/block (POST), /api/admin/slots/unblock (POST),
    /api/admin/dashboard-stats (GET)
  - Other: /api/contact (POST), /api/webhook (POST, Stripe), /api/cron/cleanup-bookings (GET)
- Server modules under src/server/modules/ (bookings, membership, admin: users, bookings, slots, dashboard)
- Shared lib: src/server/lib (stripe, recaptcha, app-url), src/server/env.ts

## Frontend structure
- Remove src/pages/ (React Router) and src/layouts/; add App Router pages under src/app/
- Feature-based UI under src/features/ (auth, booking, checkout, profile, admin, membership, landing, about)
- Retain src/components/ (layout, shared, ui), src/context/, src/hooks/
- Client API layer (src/api/) updated to call relative /api/* URLs (no separate backend URL)
- Axios client with withCredentials for cookies (src/api/client.ts)

## Removed / cleaned up
- Remove unused admin settings API (getAdminSettings, updateAdminSettings) and related client code; no UI used it
- Delete src/App.tsx, src/index.tsx, src/index.css, src/reportWebVitals.ts, src/vite-env.d.ts
- Remove src/router/, src/layouts/, src/pages/ (migrated to app/ and features/)
- Remove src/utils/axiosConfig.ts, legacy validation/input utils; add validation/api-schemas and form-schemas

## Tests
- Vitest + Testing Library retained; API route tests use mocks (auth, db, modules)
- Fix me.test.ts: mock ../../../../auth so next-auth (and next/server) not loaded in Vitest
- Fix AccountButton.test: show "My Bookings" only when user has bookings (filter in AccountButton)
- New/updated tests for API routes, cron, user/admin endpoints

## Config & deployment
- Add vercel.json with cron for /api/cron/cleanup-bookings (schedule: * * * * *; Vercel Pro for every minute)
- .env.example documents AUTH_SECRET, AUTH_URL, DATABASE_URL, Stripe, OAuth, SMTP, CRON_SECRET, etc.
